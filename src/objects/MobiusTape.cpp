#include "MobiusTape.hpp"

// MobiusTape::MobiusTape(double px, double py, double pz) : p(px, py, pz), A(0), B(0), C(0), D(0), E(0), F(0), G(0)
// {

// }
// MobiusTape::MobiusTape(double px, double py, double pz, double A, double B, double C, double D, double E, double F, double G)
// : p(px, py, pz), A(A), B(B), C(C), D(D), E(E), F(F), G(G)
// {

// }
MobiusTape::~MobiusTape(){}

// Point intersect(const Line &line) const
// {

// }
// double angleWith(const Line &line) const;
// Color getColorAt(int height, int width, int screen_height, int screenWidth, Point &intersection);